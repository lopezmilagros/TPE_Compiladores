FAC{
	ulong FACTORIAL(ulong N, cvr ulong TOTAL){
		ulong I := 3UL;
		TOTAL := 1UL;
		while (I < N) do {
			I := I + 1UL;
			TOTAL := TOTAL * I;
		}
	    return (TOTAL);
	}
	ulong NUM, RES, RESFACT;
	RESFACT:= 3UL;
	NUM := 5UL;
	RES := FACTORIAL(NUM->N,RESFACT->TOTAL); 
	print(RES);
}