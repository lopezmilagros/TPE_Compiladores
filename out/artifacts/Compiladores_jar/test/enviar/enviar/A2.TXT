PROGRAMA{
    ## Este programa contiene de alcance a verificar ##
    ulong X, DENTROM;
    ulong Y; ## X ya esta declarada ##

    X := 6UL;
    X := Y; ## Y no esta declarada ##

    ulong IMPRIMIR (ulong X){
        print(X);
        return(X);
    }

    ulong IMPRIMIR2 (ulong X){  ## Funcion ya declarada ##
        ulong DENTRO;
		print(X);
        return(X);
    }

    ulong FUNCION (ulong Y){
        ulong DENTRO;
        ulong FUNCION3 (ulong Y){
			ulong DENTRO2;
			Y := X+FUNCION.DENTRO;            ## Permitido usar x porque fue declarada en el padre ##
			return (Y);
		}
		return (DENTRO);
		
    }

    DENTROM := 7UL; ## DENTRO no esta declarada ##

    X := IMPRIMIR2(X -> Y); ## FUNCION2 no esta declarada ##



    ulong FUNCION4 (ulong H){
        print("hola");    ## funcion sin return ##
		return(H);
    }
}