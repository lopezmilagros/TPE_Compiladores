PROGRAMA1 {
    ulong X := 5;
    ulong Y := 10
    print("Inicio del programa");
    
    if (X > Y {
        print("x es mayor");
    } endif;

    while (X < Y) do {
        x := x + 1;
    }

    ulong SUMAR(ulong a, ulong b) {
        return (a + b);
    }

    Z := 3 + ; 
    A, B := 1, 2, 3;
    print(,);
    print("Fin del programa");
}

