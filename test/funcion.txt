PROGRAMA{
    ulong SUMA(ulong A ulong B) {
        return (A + B);

        ulong ADENTRO(ulong C, ulong F){
        ulong X;
            print(C);
        }
    }

    ulong(ulong A, ulong B) {
        return (A + B);
    }

    ulong SUMAR(ulong, ulong B) {
        return (B);
    };

    ulong HOLA(A, ulong B) {
        return (B);
    };

    HOLA(X -> B);
    HOLA(X -> );
}

